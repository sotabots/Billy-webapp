import cx from 'classnames'

export const PayoffMethodField =({ className }: {
  className?: string
}) => {
  return (
    <div className={cx(
      'PayoffMethodField',
      className,
    )}
    >
      ...
    </div>
  )
}
